# Awesome Cross-Framework Hooks

A complete collection of 55 popular hooks implemented in React, Angular, and Vue 3.

## 📂 Folder Structure

This repository follows a standardized structure for organizing hooks by category:

```
hooks/
├── [category]/           # e.g., animation, state-ui
│   ├── [hookName]/       # e.g., useTransition, useCounter
│   │   ├── react.ts      # React implementation
│   │   ├── angular.ts    # Angular implementation
│   │   ├── vue.ts        # Vue 3 implementation
│   │   └── info.md       # Documentation & Usage
```

### Naming Convention
- **Categories**: Lowercase (e.g., `animation`, `data-async`)
- **Hooks**: camelCase (e.g., `useFetch`, `useLocalStorage`)

## 🔗 Repository URL
[https://github.com/amit373/awesome-hooks/](https://github.com/amit373/awesome-hooks/)

## Categories

1. **State & UI Management** (`hooks/state-ui`)
2. **Data Fetching & Async** (`hooks/data-async`)
3. **Forms & Inputs** (`hooks/forms`)
4. **Auth / User** (`hooks/auth-user`)
5. **Animation & Interaction** (`hooks/animation`)
6. **Notifications** (`hooks/notifications`)
7. **Device / Connectivity** (`hooks/device`)
8. **Advanced DOM / Events** (`hooks/dom`)
